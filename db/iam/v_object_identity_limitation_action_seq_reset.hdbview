schema = "SAP_INO";
query = "select ifnull(max(id),0) + 1 as nextid from 
        (select max(id) as id from \"sap.ino.db.iam::t_object_identity_limitation_action\" 
         union all 
         select max(id) as id from \"sap.ino.db.iam::t_object_identity_limitation_action_h\" )";

depends_on_table = ["sap.ino.db.iam::t_object_identity_limitation_action",
                    "sap.ino.db.iam::t_object_identity_limitation_action_h"];  
schema = "SAP_INO";
query = "
    select id as group_id, name as group_name, description, members
    from \"sap.ino.db.iam::v_identity_full\" where type_code = 'GROUP'
    with read only 
";

depends_on_view = ["sap.ino.db.iam::v_identity_full"];
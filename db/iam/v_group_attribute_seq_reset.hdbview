schema = "SAP_INO";
query = "select ifnull(max(id),0) + 1 as nextid from 
        (select max(id) as id from \"sap.ino.db.iam::t_group_attribute\")";

depends_on_table = ["sap.ino.db.iam::t_group_attribute"]; 
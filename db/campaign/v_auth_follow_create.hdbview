schema = "SAP_INO";
query = "
		select auth.campaign_id as campaign_id
			from \"sap.ino.db.campaign::v_auth_campaigns_and_registrable\" as auth
		with read only";
 
depends_on_view = ["sap.ino.db.campaign::v_auth_campaigns_and_registrable"];
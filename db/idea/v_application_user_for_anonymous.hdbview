schema = "SAP_INO";

query = "select idea_id,
(select id as user from\"sap.ino.db.iam::v_auth_application_user\") as  app_user
from \"sap.ino.db.idea::t_ideas_setting\"

        with read only";

depends_on_table = ["sap.ino.db.idea::t_ideas_setting"];

depends_on_view = ["sap.ino.db.iam::v_auth_application_user"];
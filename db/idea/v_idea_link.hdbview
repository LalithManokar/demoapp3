schema = "SAP_INO";
query = "select id, 
				type_code, 
				object_id as idea_id,
				label, 
				url
    from \"sap.ino.db.link::t_link\"
    where object_type_code = 'IDEA' and type_code = 'URL'
    with read only";

depends_on_table = ["sap.ino.db.link::t_link"];

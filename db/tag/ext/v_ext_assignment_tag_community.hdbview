schema = "SAP_INO_EXT";
query = "select 
        ID,
        TAG_ID,
        TAG_GROUP_ID,
        OBJECT_TYPE_CODE
        SEQUENCE_NO,
        NAME,
        DESCRIPTION,
        USAGE_COUNT
from \"sap.ino.db.tag::v_assignment_tag_and_group\" with read only";

depends_on_view = ["sap.ino.db.tag::v_assignment_tag_and_group"];
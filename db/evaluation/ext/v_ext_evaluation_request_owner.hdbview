schema = "SAP_INO_EXT";
query = "select eval_req_owner.* 
            from \"sap.ino.db.evaluation::v_evaluation_request_owner\" as eval_req_owner 
            where eval_req_owner.eval_req_id in (select eval_req_id from \"sap.ino.db.evaluation::v_auth_evaluation_request_read\")
            with read only";

depends_on_view = ["sap.ino.db.evaluation::v_evaluation_request_owner","sap.ino.db.evaluation::v_auth_evaluation_request_read"];

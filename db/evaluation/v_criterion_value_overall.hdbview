schema = "SAP_INO";
query = "
    select * 
    from \"sap.ino.db.evaluation::v_criterion_value\"
    where is_overall_result = 1
    with read only
";

depends_on_view = ["sap.ino.db.evaluation::v_criterion_value"];
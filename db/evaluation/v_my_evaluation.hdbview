schema = "SAP_INO";
query = "
	select * from \"sap.ino.db.evaluation::v_evaluation\" 
	where 
		evaluator_id in (select id from \"sap.ino.db.iam::v_auth_application_user\")
    with read only";

depends_on_view = ["sap.ino.db.evaluation::v_evaluation",
                   "sap.ino.db.iam::v_auth_application_user"];
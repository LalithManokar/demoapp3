schema = "SAP_INO";
query = "SELECT CODE, 
	DEFAULT_TEXT, 
	DEFAULT_LONG_TEXT,
	ID
    FROM \"sap.ino.db.subresponsibility::t_responsibility_stage\"
    with read only
";

depends_on_table = ["sap.ino.db.subresponsibility::t_responsibility_stage"];
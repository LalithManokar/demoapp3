schema = "SAP_INO_EXT";
query = "
    select * from \"sap.ino.db.subresponsibility::v_responsibility_stage\"
    with read only
";

depends_on_view = ["sap.ino.db.subresponsibility::v_responsibility_stage"];
schema = "SAP_INO_EXT";
query = "select resp_tags.* 
            from \"sap.ino.db.subresponsibility::v_responsibility_value_tag\" as resp_tags
            with read only";

depends_on_view = ["sap.ino.db.subresponsibility::v_responsibility_value_tag"];
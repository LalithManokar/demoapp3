schema = "SAP_INO_EXT";
query = "
    select
        * 
    from
         \"sap.ino.db.basis::t_package_extension\"
         -- No instance check necessary 
    with read only
";
depends_on_table = ["sap.ino.db.basis::t_package_extension"];
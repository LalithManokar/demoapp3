schema = "SAP_INO";
query = "
    select
        version,
        version_sp,
        version_patch,
        name,
        time,
        executed_by,
        step, state
    from
        \"sap.ino.setup.db::t_log\"
";
depends_on_table = ["sap.ino.setup.db::t_log"];

schema = "SAP_INO";
query = "
select * from (
    select 
        package_id || '::' ||  object_name ||'.' ||  object_suffix as object,
        object_status 
    from _sys_repo.active_object 
    where package_id like 'sap.ino%' and object_status <> 0
) order by object asc
";

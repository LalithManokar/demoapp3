<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:ino="sap.ino.controls"
    xmlns:l="sap.ui.layout">
	<l:VerticalLayout>
		<UploadCollection
        	id="Attachments"
        	uploadUrl="{local>/ATTACHMENT_UPLOAD_URL}"
        	multiple="true"
        	showSeparators="None"
        	change="attachmentMixinOnChange"
        	beforeUploadStarts="attachmentMixinonBeforeUploadStarts"
        	fileDeleted="attachmentMixinonFileDeleted"
        	uploadComplete="attachmentMixinonUploadComplete"
        	uploadEnabled="false"
        	class="sapInoAttachmentUploadInvisible">
        	<UploadCollectionItem
          		contributor="{data>CREATED_BY_NAME}"
          		documentId="{data>ID}"
          		fileName="{data>FILE_NAME}"
          		mimeType="{data>MEDIA_TYPE}"
          		thumbnailUrl="{ parts:[{path:'data>MEDIA_TYPE'}, {path:'data>ATTACHMENT_ID'}], formatter:'.formatter.previewUrl' }"
          		uploadedDate="{ path:'data>CREATED_AT', type:'sap.ui.model.type.Date', formatOptions: { style: 'medium'}}"
          		url="{ path:'data>ATTACHMENT_ID', formatter:'.formatter.downloadUrl' }"
          		visibleEdit="false"
          		visibleDelete="false" />
		</UploadCollection>
	</l:VerticalLayout>
</core:FragmentDefinition>
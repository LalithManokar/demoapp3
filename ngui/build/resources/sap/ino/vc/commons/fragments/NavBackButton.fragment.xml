<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core">
    <Button icon="sap-icon://nav-back" 
            type="Transparent" 
            visible="{= !${device>/system/desktop} &amp;&amp; !${view>/HIDE_HEADER_BUTTONS} }"
            press="onNavBack">
        <layoutData>
            <OverflowToolbarLayoutData moveToOverflow="false" />
        </layoutData>
    </Button>
</core:FragmentDefinition>